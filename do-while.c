#include<stdio.h>

int main()
{
   int o,k, p =1 ;
   printf("what the hell is going on.");
   scanf("%d",&o);
   do
   {
      printf("              %d\n",k=o*9);
      o++;
   }
   while(k<=100);

   return 0;
}
